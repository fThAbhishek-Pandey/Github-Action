name: fth workflow
on: push 
jobs: 
 build: 
  run-ons: ubuntu-latest
   steps: 
    - uses: actions/checkout@v3
    - name: Run a sample github Action
    - run: echo "this workflow is run succesfully for this particular actions"
     
