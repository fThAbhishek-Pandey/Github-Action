name: fth workflow
on: push
jobs: 
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
      - name: Run a sample GitHub Action
        run: echo "this workflow is run successfully for this particular action"
